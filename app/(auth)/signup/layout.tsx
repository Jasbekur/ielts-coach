import { PAGE_METADATA } from "@/lib/metadata";
export const metadata = PAGE_METADATA.signup;
export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
